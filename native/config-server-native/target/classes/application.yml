server:
  port: 9090

spring:
  profiles:
    active: native
  application:
    name: native-config-server
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configs/dev




